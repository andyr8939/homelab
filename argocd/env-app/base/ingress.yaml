---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: env-web-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - host: unknown  # gets patched
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: env-web
                port:
                  number: 80