---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: env-web-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - host: k3s-2.home  # gets patched
